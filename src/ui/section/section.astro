---
import type { SectionProps } from './section.types';
const { id, step, stepText, title, epigraph, contentClassName } = Astro.props as SectionProps;
---

<section class="section" id={id}>
  <div class="section__header">
    <div class="section__title-container">
      <p class="section__step">{`${step}`.padStart(2, '0')} - {stepText}</p>

      <h2 class="section__title">{title}</h2>
    </div>

    <p class="section__epigraph">
      {epigraph}
    </p>
  </div>
  
  <div class={contentClassName}>
    <slot name="content" />
  </div>
</section>

<style lang="scss">
  .section {
    &__header {
      display: grid;
      grid-template-columns: 1fr auto;
      grid-template-areas: 'title epigraph';
      gap: 24px;
    }

    &__title-container {
      grid-area: title;
    }

    &__epigraph {
      grid-area: epigraph;
    }

    &__card-container {
      display: flex;
      flex-direction: row;
    }
  }
</style>
